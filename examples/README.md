# Graphlane Examples

当前 examples 分成两类：

- `pure_python/`
  - 最小可运行例子
  - 只依赖 in-memory 默认实现
- `redis_postgres_wiring/`
  - 展示真实 adapter 如何装到 `KernelBuilder`
  - 主要用于 wiring 参考

建议先看并先跑：

- `pure_python/run.py`

